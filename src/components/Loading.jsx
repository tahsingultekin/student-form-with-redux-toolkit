import React from "react";

const Loading = () => {
  return (
    <div className="spinner-container w-full h-screen flex justify-center items-center">
      <div className="loading-spinner"></div>
    </div>
  );
};

export default Loading;
